<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bascula.quinteros.jorgeluis.bascula_jorgeluisquinteros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Frutas_Actividad"></activity>
        <activity android:name=".Refrescos_Actividad"></activity>
        <activity android:name=".Verduras_actividad"></activity>
        <activity android:name=".Opciones"></activity>
        <activity android:name=".Ticket_Compra"></activity>
        <activity android:name=".Borrar"></activity>
    
    </application>

</manifest>